action (TestingTypeAction) {
  type (Search)
  description (Provide an overview of software testing type)
  collect {
    input (testingTypeName) {
      type (TestingTypeName)
      min (Required) max (One)
    }
  }
  output (TestingOverviewResult)
}
