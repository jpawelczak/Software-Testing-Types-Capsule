endpoints {

  action-endpoints {
    action-endpoint (TestingTypeAction) {
      accepted-inputs (testingTypeName, $vivContext)
      local-endpoint (TestingType.js)
    }
    action-endpoint (ScheduleTesting) {
      accepted-inputs (testingTypeName, relevantDateTime)
      local-endpoint (ScheduleTesting.js)
    }
  }
  
}
